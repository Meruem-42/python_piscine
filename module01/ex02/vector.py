
def dot_vertical(vector) :

class Vector() :
    def __init__(self,values, shape) :
        if not isinstance(values, list) or all(elem in values isinstance(elem, list)):
            raise TypeError("values is not a list or is not a list of list or is stocking other types than float")
        self.values = values
        self.shape = shape

    def dot(self) :
        if(self.shape == (n,1)) :
            
        else :
